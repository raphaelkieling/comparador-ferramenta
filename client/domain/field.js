export class Field {
    constructor({ order, descriptionEN, descriptionPT } = {
        order: 1,
        descriptionEN,
        descriptionPT
    }) {
        this.order = order;
        this.descriptionEN = descriptionEN;
        this.descriptionPT = descriptionPT;
    }
}
